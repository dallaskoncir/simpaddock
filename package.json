{
  "name": "simpaddock",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "storybook": "turbo run storybook",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md,json}\" --ignore-unknown"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "turbo": "^2.10.3",
    "typescript": "^6.0.3"
  }
}
